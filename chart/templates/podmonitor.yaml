{{- if .Values.metrics.podMonitor.enabled }}
{{- if not .Values.metrics.enabled }}
{{- fail "metrics.podMonitor.enabled requires metrics.enabled" }}
{{- end }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ include "multicast-relay.fullname" . }}
  namespace: {{ default (include "multicast-relay.namespace" .) .Values.metrics.podMonitor.namespace }}
  labels:
    {{- include "multicast-relay.labels" . | nindent 4 }}
    {{- with .Values.metrics.podMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  podMetricsEndpoints:
    - port: metrics
      path: {{ .Values.metrics.podMonitor.path }}
      interval: {{ .Values.metrics.podMonitor.interval }}
      {{- with .Values.metrics.podMonitor.scrapeTimeout }}
      scrapeTimeout: {{ . }}
      {{- end }}
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - {{ include "multicast-relay.namespace" . }}
  selector:
    matchLabels:
      {{- include "multicast-relay.selectorLabels" . | nindent 6 }}
{{- end }}
